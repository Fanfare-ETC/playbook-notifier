{
  "$schema": "http://json-schema.org/draft-04/schema",
  "required": ["event"],
  "properties": {
    "event": {
      "enum": [
        "client:getState",
        "operator:setState",
        "operator:createPlays",
        "operator:lockPredictions",
        "operator:clearPredictions"
      ]
    },
    "data": {}
  }
}